<#
.SYNOPSIS
    This PowerShell script ensures the system is configured to ignore NetBIOS name release requests except from WINS servers; which prevents a denial of service (DoS) attack.


.NOTES
    Author          : Sir William
    LinkedIn        : linkedin.com/in/-----------/
    GitHub          : https://github.com/sirwilliam1
    Date Created    : 2025-09-10
    Last Modified   : 2025-09-10
    Version         : 1.0
    CVEs            : N/A
    Plugin IDs      : N/A
    STIG-ID         : WN10-CC-000035

.TESTED ON
    Date(s) Tested  : 
    Tested By       : 
    Systems Tested  : 
    PowerShell Ver. : 

.USAGE
    Put any usage instructions here.
    Example syntax:
    PS C:\> .\STIG-ID-WN10-CC-000035.ps15
#>

# Define the registry path
$regPath = "HKLM:\SYSTEM\CurrentControlSet\Services\Netbt\Parameters"

# Create the registry key if it doesn't exist
if (-not (Test-Path $regPath)) {
    New-Item -Path $regPath -Force
}

# Set the NoNameReleaseOnDemand value to 1 (REG_DWORD)
New-ItemProperty -Path $regPath -Name "NoNameReleaseOnDemand" -Value 1 -PropertyType DWord -Force

Write-Output "NetBIOS name release requests are now restricted to WINS servers only."
